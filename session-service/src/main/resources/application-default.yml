server:
  port: 8085

spring:
  application:
    name: session-service

  datasource:
    url: jdbc:mysql://mysql-session:3306/skillsync_session?useSSL=false&serverTimezone=UTC   # ✅ FIXED
    username: javauser
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: rabbitmq   # ✅ FIXED
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka   # ✅ FIXED

management:
  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans   # ✅ FIXED