server:
  port: 8085

spring:
  application:
    name: session-service

  datasource:
    url: jdbc:mysql://localhost:3306/skillsync_session?useSSL=false&serverTimezone=UTC   # ✅ LOCAL FIX
    username: javauser
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost   # ✅ LOCAL FIX
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka   # ✅ LOCAL FIX

management:
  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans   # ✅ LOCAL FIX