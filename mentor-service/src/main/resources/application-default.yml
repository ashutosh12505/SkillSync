server:
  port: 8083

spring:
  application:
    name: mentor-service

  datasource:
    url: jdbc:mysql://mysql-mentor:3306/skillsync_mentor   # ✅ FIXED
    username: javauser
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka   # ✅ FIXED

management:
  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans   # ✅ FIXED