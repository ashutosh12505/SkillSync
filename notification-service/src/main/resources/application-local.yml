server:
  port: 8087

spring:
  application:
    name: notification-service

  rabbitmq:
    host: localhost   # ✅ LOCAL FIX
    port: 5672
    username: guest
    password: guest

  mail:
    host: smtp.gmail.com
    port: 587
    username: ashutosh12501@gmail.com
    password: kxjrljsibkmhqvau   # keep env-based
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka   # ✅ LOCAL FIX

management:
  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans   # ✅ LOCAL FIX